PASS 1 CONTRACT MATRIX
Goal: frontend must use real backend contracts or stay explicitly unavailable

1. Login
- Screen: /login
- Store action: useAuthStore.login(email, password)
- Endpoint: POST /api/auth/login
- Follow-up: GET /api/auth/me
- Status: Connected to real backend
- Remaining gap: Google OAuth and TOTP intentionally hidden

2. Register
- Screen: /register
- Store action: useAuthStore.register(email, password, fullName)
- Endpoint: POST /api/auth/register
- Follow-up: GET /api/auth/me
- Status: Connected to real backend
- Remaining gap: verify-email completion flow still backend stub

3. Current user / session restore
- Screen: app shell bootstrap
- Store action: useAuthStore.refreshProfile()
- Endpoint: GET /api/auth/me
- Status: Connected to real backend
- Remaining gap: refresh-token / revoke flow not fully wired in frontend UX

4. Forgot password
- Screen: /forgot-password
- Action: direct page submit
- Endpoint: POST /api/auth/forgot-password
- Status: Connected to real backend request endpoint
- Remaining gap: email delivery and final reset confirmation flow

5. Reset password confirmation
- Screen: /reset-password
- Endpoint target: POST /api/auth/reset-password
- Status: Backend stub, page intentionally status-only
- Remaining gap: token verification + password mutation

6. Verify email
- Screen: /verify-email
- Endpoint target: GET /api/auth/verify-email
- Status: Backend stub, page intentionally status-only
- Remaining gap: token verification + verified state update

7. Files root / folder listing
- Screen: /app/files
- Store action: useFileStore.loadFolder(folderId)
- Endpoint: GET /api/files/?folder_id=...
- Status: Connected to real backend

8. Folder tree
- Screen usage: move dialogs, breadcrumbs support, folder navigation
- Store action: useFileStore.refreshFolderTree()
- Endpoint: GET /api/folders/?parent_id=...
- Status: Connected to real backend

9. Create folder
- Screen: /app/files modal
- Store action: useFileStore.createFolder(...)
- Endpoint: POST /api/folders/
- Status: Connected to real backend

10. Upload file
- Screen: /app/files upload trigger
- Store action: useFileStore.uploadFiles(...)
- Endpoint: POST /api/files/upload
- Status: Connected to real backend

11. Rename file/folder
- Screen: /app/files rename modal
- Store action: useFileStore.renameItem(...)
- Endpoints:
  - PATCH /api/files/{id}/rename
  - PATCH /api/folders/{id}
- Status: Connected to real backend

12. Move file/folder
- Screen: /app/files drag-drop + move modal
- Store action: useFileStore.moveItem(...) / moveItems(...)
- Endpoints:
  - POST /api/files/{id}/move
  - PATCH /api/folders/{id}
- Status: Connected to real backend

13. Trash / restore / permanent delete
- Screens: /app/files, /app/trash
- Store actions:
  - moveToTrash / moveItemsToTrash
  - fetchTrash
  - restoreItem
  - deletePermanent
  - emptyTrash
- Endpoints:
  - DELETE /api/files/{id}
  - DELETE /api/folders/{id}
  - GET /api/trash/
  - POST /api/trash/{id}/restore
  - DELETE /api/trash/{id}/permanent
  - POST /api/trash/empty
- Status: Connected to real backend

14. Download / browser-native preview
- Screens: /app/files preview modal
- Store action: useFileStore.downloadItem(item)
- Endpoint: GET /api/files/{id}/download
- Status: Connected to real backend
- Remaining gap: generated document previews are not part of current MVP surface

15. Search
- Screen: /app/files search input
- Store action: useFileStore.searchItems(query)
- Endpoint: GET /api/search/?q=...
- Status: Connected to real backend
- Notes: now global search, not current-folder-only filtering

16. Quota
- Screens: app shell, billing
- Store action: useFileStore.refreshQuota()
- Endpoint: GET /api/files/quota
- Status: Connected to real backend
- Remaining gap: plan mutation / billing backend does not exist

17. Security settings
- Screen: /app/security
- Status: Read-only status surface
- Real backend-backed parts:
  - email verification state from GET /api/auth/me
  - forgot-password route exists
- Remaining gap:
  - TOTP / 2FA flow
  - verify-email completion
  - reset-password completion

Summary
- Core file manager is backend-backed.
- Search is backend-backed.
- Remaining non-complete areas are backend auth/billing/advanced preview gaps, not mock/demo runtime behavior.
